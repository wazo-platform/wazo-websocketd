#!/usr/bin/make -f
# -*- makefile -*-

export PYBUILD_NAME=xivo_websocketd
export PYBUILD_DESTDIR_python3=debian/xivo-websocketd/
export PYBUILD_DISABLE=test

%:
	dh $@ --with python3,systemd --buildsystem=pybuild
